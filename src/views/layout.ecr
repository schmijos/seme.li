<!doctype html>
<html lang=en>
<head>
  <meta charset=utf-8>
  <title>Semel Secure Message</title>
  <link rel="icon" href="data:image/svg+xml,<%= URI.encode_path(render("src/images/logo.svg")) %>" type="image/svg+xml" />
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      height: 100vh;

      font-family: "Arial", sans-serif;
      font-size: 18px;
      font-weight: 300;
      line-height: 1.5;
      color: rgba(0, 0, 0, 0.5);

      display: grid;
      grid-template-columns: auto minmax(100px, 684px) auto;
      grid-template-rows: auto auto auto;
      grid-template-areas:
        "header header header"
        ".      main   .     "
        "footer footer footer";
      justify-items: center;
    }

    header {
      grid-area: header;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 50px 0;
    }
    main {
      grid-area: main;
      width: 100%;
    }
    footer {
      grid-area: footer;
      align-self: end;
      margin: 50px 0;
    }

    h1 {
      font-size: 28px;
      color: rgba(0, 0, 0, 1.0);
    }
    h2 {
      margin-top: 60px;
      font-size: 18px;
      font-weight: 300;
    }
    a {
      color: #000000;
    }

    header > span {
        font-size: 18px;
        color: rgba(0, 0, 0, 0.5);
    }

    label {
      color: rgba(0, 0, 0, 1.0);
      display: block;
      margin-top: 30px;
      margin-bottom: 10px;
    }

    input[type=text], select, textarea, button {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 10px;
      box-sizing: border-box;
    }
    select {
      font-size: 16px;
      background: rgba(0, 0, 0, 0.06);
      cursor: pointer;
    }
    textarea {
      font-size: 20px;
      background: rgba(0, 0, 0, 0.06);
      resize: vertical;
    }
    button {
      font-size: 16px;
      color: white;
      background: rgba(0, 0, 0, 1.0);
      cursor: pointer;
    }
    #sharable {
      border: 1px solid #000000;
      font-size: 16px;
    }

    summary {
      cursor: pointer;
    }
    pre {
      font-size: 16px;
    }

    #form-new-header svg {
      transform: rotate(0);
    }
    #form-try-header svg {
      transform: rotate(180deg);
    }
  </style>
</head>
<script id="script-global">
  if (!window.isSecureContext) {
    alert('Insecure! Please visit with HTTPS');
    window.history.back();
  }
</script>
<body>

<%= content %>

<footer>
  <p><em>Semper Semel Simplex</em></p>
</footer>
</body>
</html>
