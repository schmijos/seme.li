<!doctype html>
<html lang=en>
<head>
  <meta charset=utf-8>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Renuo Secrets</title>
  <link rel="icon" href="data:image/svg+xml,<%= URI.encode_path(render("src/images/logo.svg")) %>" type="image/svg+xml" />
  <style>
   :root {
      --main-color: rgb(24, 129, 94);
    }
    * {
      box-sizing: border-box;
      font-family: "Arial", sans-serif;
      font-size: 16px;
      font-weight: 300;
      line-height: 1.5;
      color: rgba(0, 0, 0, 0.5);
    }

    body {
      margin: 0;
      height: 100vh;

      display: grid;
      grid-template-columns: auto minmax(100px, 684px) auto;
      grid-template-rows: auto auto auto;
      grid-template-areas:
        "header header header"
        ".      main   .     "
        "footer footer footer";
      justify-items: center;
    }

    header {
      grid-area: header;
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 121px 0 81px 0;
    }
    main {
      grid-area: main;
      width: 100%;
    }
    footer {
      grid-area: footer;
      align-self: end;
      margin: 121px 0;
    }

    h1 {
      font-size: 28px;
      color: var(--main-color);
    }
    h2 {
      font-size: 18px;
      font-weight: 300;
    }
    a {
      color: var(--main-color);
    }

    header > span {
      font-size: 18px;
      color: rgba(0, 0, 0, 0.5);
    }

    label {
      color: rgba(0, 0, 0, 1.0);
      display: block;
      font-size: 18px;
      margin-top: 50px;
      margin-bottom: 10px;
    }

    input[type=text], select, textarea, button {
      width: 100%;
      padding: 12px;
      background-color: rgba(0, 0, 0, 0.06);
      border: none;
      border-radius: 10px;
      box-sizing: border-box;
    }
    input[type=text] {
      margin-bottom: 12px;
    }
    select {
      cursor: pointer;
      height: 51px;
    }
    textarea {
      resize: vertical;
    }
    button {
      color: white;
      background-color: var(--main-color);
      color: rgb(233, 251, 245);
      cursor: pointer;
      height: 51px;
    }
    #sharable-container button {
      background: #F0F0F0;
      border: 1px solid rgba(0, 0, 0, 0.2);
      color: rgba(0, 0, 0, 0.75);
      font-size: 14px;
      padding: 10px;
      height: 37px;
      line-height: 17px;
    }
    #sharable-container textarea {
      margin: 5px 0;
      font-family: monospace;
    }
    #explanation {
      margin-top: 100px;
    }

    summary {
      cursor: pointer;
    }
    pre {
      font-family: monospace;
      white-space: pre-wrap;
    }

    #form-new-header svg {
      transform: rotate(0);
    }
    #form-try-header svg {
      transform: rotate(180deg);
    }

    input[type="text"], select, textarea {
      outline: none;
    }
    input[type="text"]:focus, select:focus, textarea:focus {
      color: #000000;
      box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.9);
      transition: box-shadow 0.3s ease-in-out;
    }

    select {
      appearance: none;
    }
    .select-icon-hack {
        position: relative;
    }
    .select-icon-hack:after {
      content: "▼";
      color: rgba(0, 0, 0, 0.25);
      position: absolute;
      right: 16px;
      top: 13px;
      pointer-events: none;
      transform: scaleY(0.8);
    }
  </style>
</head>
<script id="script-global">
  if (!window.isSecureContext) {
    alert('Insecure! Please visit with HTTPS');
    window.history.back();
  }
</script>
<body>

<%= content %>

<footer>
  <p>
  Provided by <a href="https://renuo.ch">Renuo AG</a>. 
  <a href="https://github.com/schmijos/seme.li">Original project on GitHub</a>
  </p>
</footer>
</body>
</html>
